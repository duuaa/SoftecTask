import { environment } from "src/environments/environment";

export const usersAPI = environment.APIURL + "/users"
export const productsAPI = environment.APIURL + "/products"
export const ordersAPI = environment.APIURL + "/orders"
